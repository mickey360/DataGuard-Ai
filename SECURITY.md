# Security notes

DataGuard is designed around data minimization. The AI investigator receives schema and statistical evidence rather than raw records. Production deployments must add authentication, authorization, secret storage, encryption, rate limits, audit logging and tenant isolation before processing sensitive company data.
